@model IEnumerable<Neo4jClient.Node<MusicHouse.Models.Genre>>

<p class="pull-left">
    @Html.ActionLink("Add Genre", "Create", new { id = 1 }, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Data.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data.Origin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Data.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data.Origin)
        </td>
        <td>
            <a href="@Url.Action("Edit", "Genre", new { id = item.Reference.Id })" class="">
                <i class="glyphicon glyphicon-edit"></i>
                <span>
                    <strong>Edit</strong>
                </span>
            </a>
            |
            <a href="@Url.Action("Delete", "Genre", new { id = item.Reference.Id })" class="">
                <i class="glyphicon glyphicon-remove"></i>
                <span>
                    <strong>Delete</strong>
                </span>
            </a>
            @*@Html.ActionLink("Edit", "Edit", new { id = item.Reference.Id }) |*@
            @*@Html.ActionLink("Details", "Details", new { id = item.Reference.Id }) |*@
            @*@Html.ActionLink("Delete", "Delete", new { id = item.Reference.Id })*@
        </td>
    </tr>
}

</table>
